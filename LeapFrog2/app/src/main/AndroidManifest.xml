<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dns.leapfrog2">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".application.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:theme="@style/NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ListActivity"
            android:label="@string/app_list" />
        <activity
            android:name=".MenuListActivity"
            android:label="@string/menu_list" />
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_login" />
        <activity
            android:name=".DataPassActivity"
            android:label="@string/app_datapass" />
        <activity android:name=".CustomListView" />
        <activity android:name=".Style_Example" />
        <activity android:name=".LoginPractice1" />
        <activity android:name=".Custom_Fonts" />
        <activity android:name=".FragmentActivityExample" />
        <activity android:name=".ViewPagerActivity" />
        <activity
            android:name=".MaterialDesignTabs"
            android:label="@string/title_activity_parallax_scrolling_tabs"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".JsonActivity"
            android:label="@string/title_activity_json"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>
